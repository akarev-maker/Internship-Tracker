# Your skillset. Edit freely — higher weight = matters more to you.
# Changing this file re-scores every posting once (the cache keys on its hash).

# Your résumé is personal, so this public file leaves it blank: set the real
# text in the PROFILE_RESUME environment variable (a GitHub Actions secret),
# which overrides whatever is here. Filling this in works too — it just
# commits your résumé to the repo. Only Gemini scoring reads it; the
# [weights] below drive the deterministic score either way.
#
# Example of the shape it expects (one prose paragraph):
#   resume = """
#   Early-undergrad CS student. Strongest in web-application exploitation
#   (XSS, SQL injection, file upload, API attacks). Pentest fundamentals
#   (Nmap, Metasploit, enumeration). Blue-team basics (SIEM, incident
#   handling, log analysis). Python/FastAPI engineering. Based in Boston.
#   """
resume = ""

[weights]                 # deterministic keyword floor
"web application" = 5
"web app" = 5
xss = 5
"sql injection" = 5
sqli = 5
"file upload" = 4
"api" = 4
burp = 4
proxy = 3
pentest = 4
"penetration test" = 4
nmap = 3
metasploit = 3
enumeration = 3
python = 3
fastapi = 3
bash = 2
linux = 2
ctf = 3
siem = 2
"incident response" = 2
"incident handling" = 2
"traffic analysis" = 2
"log analysis" = 2
"blue team" = 2
"red team" = 3
